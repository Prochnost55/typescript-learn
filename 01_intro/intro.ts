// let's define an user object having name and age
let user = {
    name: "Abhishek",
    age: 25
}

// this will work fine
console.log(user.name); // Abhishek

// if we pring user.email the IDE will not show any issue but we will be getting undefined.
console.log(user.email);
